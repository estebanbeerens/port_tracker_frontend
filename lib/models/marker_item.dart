class MarkerItem {
  int id;
  String title;
  String deviceOrFirm;
  String icon;
  double lat;
  double lng;
  bool isLoad;
  MarkerItem(this.id, this.title, this.deviceOrFirm, this.icon, this.lat, this.lng, this.isLoad);
}